server:
  port: 8000

  servlet:
    context-path: /
spring:
  main:
     allow-bean-definition-overriding: true
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/dados?serverTimezone=UTC&createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
        ddl-auto: update
        properties.hibernate.dialect: org.hibernate.dialect.MySQL5InnoDBDialect
